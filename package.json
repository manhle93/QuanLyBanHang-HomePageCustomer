{
  "name": "customer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.12.0",
    "nuxt": "^2.14.0",
    "pm2": "^4.5.1",
    "push-dir": "^0.4.1",
    "vue-simplemde": "^1.1.2"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.11.2"
  },
  "config": {
    "nuxt": {
      "port": "9000"
    }
  }
}
